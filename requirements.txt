# PolyNetAI 项目依赖
# 安装: pip install -r requirements.txt
# 建议在项目根目录执行上述命令（与 pyproject.toml 同级）

# ---- 运行时依赖（与 pyproject.toml [project.dependencies] 一致）----
openpyxl>=3.1
pandas>=2.2
plotly>=5.0
PyYAML>=6.0
websocket-client>=1.8
requests>=2.28.0
py-clob-client>=0.34.6

# ---- 自动赎回 Relayer（与 pyproject.toml [project.optional-dependencies] redeem 一致）----
web3>=6.0.0
py-builder-relayer-client>=0.0.1
py-builder-signing-sdk>=0.0.1

# ---- 开发 / 测试（运行 pytest 时需要）----
pytest>=8.0
